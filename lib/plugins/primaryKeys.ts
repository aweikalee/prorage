import { createPluginPrimaryKey } from '../utils'

export const expires = createPluginPrimaryKey('expires')
export const objects = createPluginPrimaryKey('objects')
export const primitives = createPluginPrimaryKey('primitives')
